<snippet>
	<content><![CDATA[
${1:/boot/grub/grub.conf}:
	file.symlink:
		- target: ${2:/etc/grub.conf}
		- makedirs: ${3:False}

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>file-symlink</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.sls</scope>
	<description>Create a symlink (SaltStack).</description>
</snippet>
